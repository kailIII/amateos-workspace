<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.inftel.socialwind.client.mobile"
    android:versionCode="11"
    android:versionName="1.0" >

    <!-- C2DM requires Android SDK version 2.2 (API level 8) or above -->
    <uses-sdk android:minSdkVersion="8" />

    <!-- Only this application can receive the messages and registration result -->
    <permission
        android:name="org.inftel.socialwind.client.mobile.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="org.inftel.socialwind.client.mobile.permission.C2D_MESSAGE" />

    <!-- Permission to register and receive data messages -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Permission for internet access -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permission for check internet access -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permission to keep the processor from sleeping -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permission to retrieve the list of accounts in the Accounts Service -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- Permission to request authtokens from the AccountManager -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    
    <!-- Permisos para acceso a la localizacion -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />

    <application
        android:icon="@drawable/app_icon"
        android:label="@string/app_name" >

        <!-- Boot activity -->
        <activity
            android:name=".activities.SocialWindActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tabs activity -->
        <activity
            android:name=".activities.TabsActivity"
            android:label="SocialWind" >
        </activity>

        <!-- Tabs activity -->
        <activity
            android:name=".activities.HotspotsActivity"
            android:label="Hotspots"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- Tabs activity -->
        <activity
            android:name=".activities.SessionsActivity"
            android:label="Historial"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- Tabs activity -->
        <activity
            android:name=".activities.ProfileActivity"
            android:label="Surfer"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>

        <!-- Account selection activity -->
        <activity
            android:name="org.inftel.socialwind.client.mobile.activities.AccountsActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>

        <!--
             In order to use the C2DM library, an application must declare a class 
             with the name C2DMReceiver, extending com.google.android.c2dm.C2DMBaseReceiver.
             It must also include this section in the manifest.
        -->
        <service android:name="org.inftel.socialwind.client.mobile.comm.C2DMReceiver" />

        <!--
             Only Google services can send data messages for the app. If permission
             is not set - any other app can generate it
        -->
        <receiver
            android:name="com.google.android.c2dm.C2DMBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >

            <!-- Receive the actual message -->
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />

                <category android:name="org.inftel.socialwind.client.mobile" />
            </intent-filter>
            <!-- Receive the registration id -->
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="org.inftel.socialwind.client.mobile" />
            </intent-filter>
        </receiver>
    </application>

</manifest>